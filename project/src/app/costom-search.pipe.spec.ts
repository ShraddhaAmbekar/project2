import { CostomSearchPipe } from './costom-search.pipe';

describe('CostomSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CostomSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
